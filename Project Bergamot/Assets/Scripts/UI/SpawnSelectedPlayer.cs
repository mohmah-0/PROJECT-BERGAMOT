using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnSelectedPlayer : MonoBehaviour
{
    /*public GameObject playerSelected;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void GetPlayerCharacter()
    {
        playerSelected = playerSelection.selectedChar.gameObject;
        Instantiate(playerSelected);
    }*/
}
